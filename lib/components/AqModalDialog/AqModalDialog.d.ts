import React from "react";
export interface AqModalDialogProps {
    visible: boolean;
    children?: React.ReactNode | React.ReactNode[];
}
export declare function AqModalDialog({ visible, children }: AqModalDialogProps): import("react/jsx-runtime").JSX.Element;
